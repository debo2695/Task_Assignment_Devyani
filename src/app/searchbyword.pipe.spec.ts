import { SearchbywordPipe } from './searchbyword.pipe';

describe('SearchbywordPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbywordPipe();
    expect(pipe).toBeTruthy();
  });
});
